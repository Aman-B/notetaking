<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.shreya.notetaking.MainActivity"
    android:background="?android:attr/detailsElementBackground">

    <Button
        android:text="Make a note!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="143dp"
        android:id="@+id/makenote"
        android:background="@color/colorAccent"
        android:elevation="10dp"
        android:textColor="#FFFFFF"
        android:padding="5dp" />

    <Button
        android:text="View Notes"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/makenote"
        android:layout_marginTop="48dp"
        android:id="@+id/viewnotes"
        android:layout_alignLeft="@+id/makenote"
        android:layout_alignStart="@+id/makenote"
        android:layout_alignRight="@+id/makenote"
        android:layout_alignEnd="@+id/makenote"
        android:elevation="10dp"
        android:background="@color/colorAccent"
        android:textColor="#FFFFFF" />

</RelativeLayout>
